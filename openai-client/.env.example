# OpenAI Client Mode Configuration
# Uses OpenAIChatClient - connects directly to Azure OpenAI or OpenAI endpoints
# No Foundry project required (unlike AzureAIClient mode)

# ============================================
# AUTH PRIORITY (first match wins)
# ============================================
# 1. AZURE_OPENAI_ENDPOINT + APIM_SUBSCRIPTION_KEY  (Foundry via APIM gateway)
# 2. AZURE_OPENAI_ENDPOINT + AZURE_OPENAI_API_KEY   (Azure OpenAI with API key)
# 3. AZURE_OPENAI_ENDPOINT only                      (Managed Identity - recommended for Azure)
# 4. OPENAI_API_KEY                                  (Direct OpenAI API)

# ============================================
# OPTION 1: Azure OpenAI with Managed Identity (recommended for containers)
# ============================================
# Just set the endpoint - auth uses DefaultAzureCredential
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_VERSION=2024-05-01-preview

# Optional: For user-assigned managed identity, set the client ID
# AZURE_CLIENT_ID=your-managed-identity-client-id

# ============================================
# OPTION 2: Azure OpenAI with API Key (fallback for local dev or non-Azure containers)
# ============================================
# AZURE_OPENAI_API_KEY=your-azure-openai-api-key

# ============================================
# OPTION 3: Direct OpenAI API
# ============================================
# OPENAI_API_KEY=sk-...

# ============================================
# MODEL CONFIGURATION
# ============================================
AZURE_AI_MODEL_DEPLOYMENT_NAME=gpt-4o

# ============================================
# APPLICATION CONFIGURATION
# ============================================
TODO_API_URL=https://jsonplaceholder.typicode.com/todos

# ============================================
# TRACING (REQUIRED for Application Insights)
# ============================================
# Since we're NOT using a Foundry project, APPLICATIONINSIGHTS_CONNECTION_STRING
# is REQUIRED to export traces to Application Insights.
# Get this from Azure Portal -> Application Insights -> Properties
APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=...;IngestionEndpoint=...

# Service name for OpenTelemetry tracing
OTEL_SERVICE_NAME=todo-agent

# Agent ID for tracing (appears as gen_ai.agent.id in Application Insights)
AGENT_ID=TodoAgent
